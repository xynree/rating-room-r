type Item = {
  id: number;
  name: string;
  description: string;
  comments: string;
  date: string;
};

type Category = {
  id: number;
  name: string;
  description: string;
};

type Rating = {
  id: number;
  rating: number;
};
