type Item = {
  itemId: number;
  name: string;
  description: string;
  comments: string;
  date: string;
};

type Category = {
  category_id: number;
  name: string;
  description: string;
};

type Rating = {
  ratingId: number;
  rating: number;
};
